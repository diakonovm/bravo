export default function SearchExplorer({ className }) {
  return (
    <div className={`h-full flex flex-col ${className}`}>
      <div className="flex-1 min-h-0 py-4 overflow-y-scroll">search</div>
    </div>
  )
}
